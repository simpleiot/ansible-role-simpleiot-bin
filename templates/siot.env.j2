# Simple IoT service environment, installed by the simpleiot-bin Ansible role.
# The token grants full access to this instance, so the file is readable by the
# service user only.
{% if siot_auth_token != "" %}
SIOT_AUTH_TOKEN={{ siot_auth_token }}
{% endif %}
